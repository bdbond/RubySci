<div class="span-24 header round">
    <b>RubySci :: NE 491 :: Glow Discharge</b>
    <% if @current_user %>
    <div style="float:right;">Logged in as <%= @current_user.name %><%= admin? ? " (Admin)" : "" %>
    <%= link_to "Log out",logout_path %>
    </div>
    <% end %>
</div> 